# Basic Genome Generator

A simple CLI tool to generate random genome sequnce in FASTA format

## Usage:
- Install package - `poetry install`
- Generate Read - `poetry run generate-genome --length {len} --output {path} [--wrap {wrapping}]`